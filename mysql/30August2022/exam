## DELETE Trigger Code ##
DELETE FROM product WHERE manufacturer_id=OLD.id 

## Procedure ##
INSERT INTO manufacturer VALUES (NULL, m_name, m_address, m_contact)

## procedure graphically ## 

CREATE PROCEDURE `add_manufacturer`(IN `m_name` VARCHAR(30), IN `m_address` VARCHAR(50), IN `m_contact` VARCHAR(20)) NOT DETERMINISTIC CONTAINS SQL SQL SECURITY DEFINER INSERT INTO manufacturer VALUES (NULL, m_name, m_address, m_contact) 
